import { PageHeader } from "@/components/page-header";
import { User } from "lucide-react";

export default function Maps() {
    return (
        <div>
            <PageHeader path="Clientes /" title="Mapas" icon={<User />} />
        </div>
    )
}