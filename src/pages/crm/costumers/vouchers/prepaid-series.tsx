import { PageHeader } from "@/components/page-header";
import { User } from "lucide-react";

export default function PrepaidSeries() {
    return (
        <div>
            <PageHeader path="Clientes / Vouchers /" title="Séries pré-pagas" icon={<User />}/>
        </div>
    )
}